# Copyright (c) 2022, salesforce.com, inc.
# All rights reserved.
# SPDX-License-Identifier: BSD-3-Clause
# For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause

datasets:
  qvh: # name of the dataset builder
    data_type: videos # [images|videos|features]
    build_info:
      # Be careful not to append minus sign (-) before split to avoid itemizing
      annotations:
        train:
          url: /content/lavis/train.json
          storage: /content/lavis/train.json
        val:
          url: /content/lavis/val.json
          storage: /content/lavis/val.json
        test:
          url: /content/lavis/test_dummy.json
          storage: /content/lavis/test_dummy.json
      videos:
        storage: /content/data/raw/QVHighlights
